# Highgate Piano Lessons

A self-contained static website, ready for GitHub Pages.

## Publish

1. Create a GitHub repository and upload this folder's contents.
2. In the repository, open **Settings → Pages**.
3. Set the source to **Deploy from a branch**, choose `main` and `/ (root)`, then save.

The existing Google contact form remains connected through the “Send a message” button.
